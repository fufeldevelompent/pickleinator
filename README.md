# pickleinator

Can pickles by timer. Handles brine ratios, jar rotations and the existential
dread of an empty jar.

## Build
```
dotnet build -c Release
dotnet run --project src/Pickleinator -- --batch 12
```
